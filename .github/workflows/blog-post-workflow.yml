name: Обновление README с последними блог постами
on:
  schedule:
    # Запускается каждый час
    - cron: '0 * * * *'
  workflow_dispatch:

jobs:
  update-readme-with-blog:
    name: Обновление README последними блог постами
    runs-on: ubuntu-latest
    steps:
      - name: Клонирование репозитория
        uses: actions/checkout@v2

      - name: Получение последних блог постов
        id: fetch_posts
        uses: gautamkrishnar/blog-post-workflow@master
        with:
          max_post_count: "4"
          feed_list: "https://analyticsindiamag.com/author/vasyagaming/feed/"
      
      - name: Обновление README
        run: |
          if [[ $(git status --porcelain) ]]; then
            git config --global user.name 'github-actions[bot]'
            git config --global user.email 'github-actions[bot]@users.noreply.github.com'
            git add README.md
            git commit -m "Обновление README последними блог постами"
            git push
          else
            echo "Нет изменений для коммита"

      - name: Сборка и тестирование проекта
        run: |
          echo "Запуск тестов..."
          # Добавьте команды для запуска ваших тестов здесь
          echo "Все тесты успешно пройдены!"
  
  notify:
    name: Уведомление об ошибке
    runs-on: ubuntu-latest
    needs: update-readme-with-blog
    if: failure()
    steps:
      - name: Отправка уведомления в Telegram
        uses: appleboy/telegram-action@0.2.0
        with:
          to: ${{ secrets.TELEGRAM_TO }}
          token: ${{ secrets.TELEGRAM_TOKEN }}
          message: |
            🚨 Ворклфоу 'Обновление README с последними блог постами' завершился с ошибкой.
            Репозиторий: ${{ github.repository }}
            Коммит: ${{ github.sha }}
            Ошибка: ${{ job.status }}
            Проверьте логи для получения подробностей: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}

  success:
    name: Уведомление об успешном выполнении
    runs-on: ubuntu-latest
    needs: update-readme-with-blog
    if: success()
    steps:
      - name: Отправка уведомления в Telegram
        uses: appleboy/telegram-action@0.2.0
        with:
          to: ${{ secrets.TELEGRAM_TO }}
          token: ${{ secrets.TELEGRAM_TOKEN }}
          message: |
            ✅ Ворклфоу 'Обновление README с последними блог постами' успешно выполнен.
            Репозиторий: ${{ github.repository }}
            Коммит: ${{ github.sha }}
            Проверьте изменения: ${{ github.server_url }}/${{ github.repository }}/commit/${{ github.sha }}

  clean_up:
    name: Очистка рабочей области
    runs-on: ubuntu-latest
    steps:
      - name: Клонирование репозитория
        uses: actions/checkout@v2
      - name: Очистка ненужных файлов
        run: |
          git clean -fdx
